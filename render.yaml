services:
  - type: web
    name: signtusk-nextjs
    env: node
    buildCommand: bash scripts/build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DISABLE_YARN
        value: "true"